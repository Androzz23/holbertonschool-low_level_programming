#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main (void)
{
char l = 'a';
char f = 'z';

while (l <= f)
{
putchar(l);
l++;
}

 return (0);
}
